[package]
name = "auxide-io"
version = "0.1.0"
edition = "2021"

[dependencies]
auxide = { path = "../auxide" }
cpal = "0.15"
anyhow = "1.0"

[features]
jack = ["cpal/jack"]
[profile.release]
panic = "abort"
